class Solution {
    public int[] constructTransformedArray(int[] nums) {
        final int n=nums.length;
        int[] answer=new int[n];
        for(int i=0;i<n;++i){
            answer[i]=nums[(i+(nums[i]%n+n)%n)%n];
        }
        return answer;
    }
}
